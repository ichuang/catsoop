to build, install the vsce package:
    $ npm install -g @vscode/vsce

then run the following from this directory:
    $ vsce package

or download pre-build extension from https://catsoop.org/_static/catsoop-syntax-0.0.1.vsix

to install, run:
    $ code --install-extension catsoop-syntax-0.0.1.vsix
