## Vim Syntax Highlighting for catsoop Files

This directory contains syntax highlighting definitions for `.catsoop` files
in Vim.

To use, simply copy the contents of the `plugin` and `syntax` directories into
the corresponding directories in `~/.vim`.
