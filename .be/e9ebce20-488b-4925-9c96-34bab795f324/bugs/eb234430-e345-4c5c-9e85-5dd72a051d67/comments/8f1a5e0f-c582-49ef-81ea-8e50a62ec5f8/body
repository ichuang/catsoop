It would be nice if `pythoncode` checkers had access to more than just the log
entries we create.  In particular, it would be good for them to have access to
`stdout` and `stderr` as well.    This will probably have to be carefully
orchestrated, as a lot of courses use `pythoncode` questions.
